
#########################
# Special Workspace
#########################
bind = SUPER, n, togglespecialworkspace, obsidian

workspace = special:obsidian, {
  on-created-empty = xdg-open "obsidian://daily?vault=grizzlyweb"
  # weitere optionen:
  gapsin = 10
  gapsout = 20
  border = true
}

windowrule {
  name = obsidian-scratch
  match:class = ^(obsidian)$

  workspace = special:obsidian
  float = on
  center = on
  size = (monitor_w*0.75) (monitor_h*0.90)
  opacity = 0.95
}


bind = SUPER, G, togglespecialworkspace, gaming
