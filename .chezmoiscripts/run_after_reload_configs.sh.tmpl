#!/usr/bin/env bash
# [chezmoi] Placeholder für ${script}

set -euo pipefail

echo "[chezmoi] Reloading Hyprland session services and configs..."

# Restart user services
for svc in mako hypridle; do
    systemctl --user restart "${svc}.service"
done

hyprpanel -q; hyprpanel &

echo "[chezmoi] hyprctl reload"
pypr reload
hyprctl reload
